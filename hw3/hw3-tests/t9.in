int myfunc(int x) {
    x = x + 3;
    return x;
}

int return6(const int x) {
    int y = myfunc(x);
    return 6;
}

void main() {
    int a = 5;
    byte b = 300;
    int c = a + b;
    bool c = false or true;
    if (not c) {
        int x = 6;
        printi(return6(x));
    }
    else
        syntax error;
}
